import {
  Swiper,
  Navigation,
  Pagination,
  Controller,
  EffectFade,
  Scrollbar,
  // Autoplay,
  // Parallax,
} from 'swiper/dist/js/swiper.esm';

Swiper.use([
  Navigation,
  Pagination,
  Controller,
  EffectFade,
  Scrollbar,
  // Autoplay,
  // Parallax,
]);

export default Swiper;
